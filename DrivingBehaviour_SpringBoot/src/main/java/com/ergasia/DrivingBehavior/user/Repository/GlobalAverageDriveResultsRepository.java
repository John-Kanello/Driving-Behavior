package com.ergasia.DrivingBehavior.user.Repository;

import com.ergasia.DrivingBehavior.user.Classes.GlobalAverageDriveResults;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GlobalAverageDriveResultsRepository extends JpaRepository<GlobalAverageDriveResults, Long> {
}
